#include <stdio.h>
#include <string.h>

int main(){
    char s[100];
    gets(s);
    printf("%s", strrev(s));
    return 0;
}


WITHOUT...
#include <stdio.h>

int main(){
    char s[100];
    int i=0;

    gets(s);

    while(s[i] != '\0')
        i++;

    while(i >= 0)
        printf("%c", s[i--]);

    return 0;
}