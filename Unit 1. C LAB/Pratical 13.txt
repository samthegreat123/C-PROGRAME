#include <stdio.h>
#include <string.h>

int main(){
    char s[100];

    printf("Enter a string: ");
    fgets(s, sizeof(s), stdin);

    printf("Length = %lu", strlen(s)-1);

    return 0;
}


WITHOUT...

#include <stdio.h>

int main(){
    char s[100];
    int i = 0;

    printf("Enter a string: ");
    fgets(s, sizeof(s), stdin);

    while(s[i] != '\0' && s[i] != '\n')
        i++;

    printf("Length = %d", i);

    return 0;
}