#include <stdio.h>

struct Complex{
    float real;
    float imag;
};

// Function to add two complex numbers
struct Complex add(struct Complex c1, struct Complex c2){
    
    struct Complex result;

    result.real = c1.real + c2.real;
    result.imag = c1.imag + c2.imag;

    return result;
}

int main(){

    struct Complex c1, c2, sum;

    // Input
    printf("Enter real and imaginary part of first number: ");
    scanf("%f %f", &c1.real, &c1.imag);

    printf("Enter real and imaginary part of second number: ");
    scanf("%f %f", &c2.real, &c2.imag);

    // Function call
    sum = add(c1, c2);

    // Output
    printf("\nSum = %.2f + %.2fi\n", sum.real, sum.imag);

    return 0;
}
.................................OUTPUT:
Enter real and imaginary part of first number: 2 3
Enter real and imaginary part of second number: 4 5

Sum = 6.00 + 8.00i